# Raw RSS must retain the exact downloaded bytes used by its manifest SHA-256.
data/inbox/**/feed.xml -text

# Keep the verified renderer bundle byte-identical on Windows and Unix.
src/vendor/mathjax/* text eol=lf
